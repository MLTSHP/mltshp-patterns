import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import defaultMarkup from './examples/dashboard-new-user.html';

<Meta title="Legacy/Components/New User Dashboard" />

# New User Dashboard

Appears on the homepage if the user has no shared files to view.

<Preview>
  <Story name="New User Dashboard" height="900px">
    {() => defaultMarkup}
  </Story>
</Preview>
