import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import defaultMarkup from './examples/feature-default.html';
import flippedMarkup from './examples/feature-flipped.html';
import primaryMarkup from './examples/feature-primary.html';
import finaleMarkup from './examples/feature-finale.html';
import listMarkup from './examples/feature-list.html';

<Meta title="Legacy/Components/Features" />

# Features

Used on the logged-out version of the homepage to promote site features.

Note the responsive layout for the features doesn't display quite right in the Docs view, so you may want to switch back to the Canvas view for this component.

## Default

<Canvas>
  <Story name="Default">{() => defaultMarkup}</Story>
</Canvas>

## Flipped

<Canvas>
  <Story name="Flipped">{() => flippedMarkup}</Story>
</Canvas>

## Primary

<Canvas>
  <Story name="Primary">{() => primaryMarkup}</Story>
</Canvas>

## Finale

<Canvas>
  <Story name="Finale">{() => finaleMarkup}</Story>
</Canvas>

## Feature List

<Canvas>
  <Story name="Feature List">{() => listMarkup}</Story>
</Canvas>
