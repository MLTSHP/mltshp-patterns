import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import loggedOutMarkup from './examples/logged-out.html';
import loggedInMarkup from './examples/logged-in.html';

<Meta title="Legacy/Components/Site Header" />

# Site Header

Note that the Site Header includes the Site Navigation, Choose a Shake, and User Navigation components.

## Logged Out

<Preview>
  <Story
    name="Logged Out"
    height="200px"
    parameters={{
      themes: [
        {
          name: 'Blue',
          class: 't-blue',
          default: true,
        },
      ],
    }}
  >
    {() => loggedOutMarkup}
  </Story>
</Preview>

## Logged In

<Preview>
  <Story
    name="Logged In"
    height="200px"
    parameters={{
      themes: [
        {
          name: 'Blue',
          class: 't-blue',
          default: true,
        },
      ],
    }}
  >
    {() => loggedInMarkup}
  </Story>
</Preview>
