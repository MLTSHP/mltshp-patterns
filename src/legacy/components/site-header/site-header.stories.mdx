import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import loggedOutMarkup from './examples/logged-out.html';
import loggedInMarkup from './examples/logged-in.html';
const themes = [
  { name: 'Blue', class: 't-blue', color: '#dbfaff', default: true },
  { name: 'Dark', class: 't-dark', color: '#263238' },
];

<Meta title="Legacy/Components/Site Header" />

# Site Header

Note that the Site Header contains the [Site Navigation](/?path=/docs/legacy-components-site-navigation--logged-in), [Choose a Shake](/?path=/docs/legacy-components-choose-a-shake--collapsed), and [User Navigation](/?path=/docs/legacy-components-user-navigation--logged-in) components. See those components for variations and states.

## Logged Out

<Canvas>
  <Story name="Logged Out" height="200px" parameters={{ themes }}>
    {() => loggedOutMarkup}
  </Story>
</Canvas>

## Logged In

<Canvas>
  <Story name="Logged In" height="200px" parameters={{ themes }}>
    {() => loggedInMarkup}
  </Story>
</Canvas>
