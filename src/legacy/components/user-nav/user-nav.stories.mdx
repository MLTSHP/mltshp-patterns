import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import userNavLoggedInMarkup from './examples/user-nav-logged-in.html';
import userNavLoggedOutMarkup from './examples/user-nav-logged-out.html';
import confirmEmailMarkup from './examples/confirm-email.html';
import maintenanceMarkup from './examples/maintenance.html';
const themes = [
  { name: 'Blue', class: 't-blue', color: '#dbfaff', default: true },
  { name: 'Dark', class: 't-dark', color: '#263238' },
];

<Meta title="Legacy/Components/User Navigation" />

# User Navigation

Appears in the [Site Header](/?path=/docs/legacy-components-site-header--logged-out).

## Logged In

<Canvas>
  <Story name="Logged In" height="150px" parameters={{ themes }}>
    {() => userNavLoggedInMarkup}
  </Story>
</Canvas>

## Logged Out

<Canvas>
  <Story name="Logged Out" height="50px" parameters={{ themes }}>
    {() => userNavLoggedOutMarkup}
  </Story>
</Canvas>

## Confirm Your Email

<Canvas>
  <Story name="Confirm Your Email" height="50px" parameters={{ themes }}>
    {() => confirmEmailMarkup}
  </Story>
</Canvas>

## Maintenance Mode

<Canvas>
  <Story name="Maintenance Mode" height="50px" parameters={{ themes }}>
    {() => maintenanceMarkup}
  </Story>
</Canvas>
