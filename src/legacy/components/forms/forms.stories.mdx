import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import funFormMarkup from './examples/fun-form.html';
import funFormStackedMarkup from './examples/fun-form-stacked.html';
import funFormErrorsMarkup from './examples/fun-form-errors.html';
import formFieldsMarkup from './examples/form-fields.html';

<Meta title="Legacy/Components/Forms" />

# Media

## Fun Form

This is our default form style. Note that it expects to be used in a constrained width container.

<Canvas>
  <Story name="Fun Form" parameters={{ layout: 'fullscreen' }}>
    {() => funFormMarkup}
  </Story>
</Canvas>

## Fun Form Stacked

A vertically-stacked modifier for Fun Form. Useful for width-constrained areas like the sidebar.

<Canvas>
  <Story name="Fun Form Stacked" parameters={{ layout: 'fullscreen' }}>
    {() => funFormStackedMarkup}
  </Story>
</Canvas>

## Fun Form Errors

List of Errors that usually lives above the form.

<Canvas>
  <Story name="Fun Form Errors">{() => funFormErrorsMarkup}</Story>
</Canvas>

## Form Fields

Unstyled native form elements for reference.

<Canvas>
  <Story name="Form Fields">{() => formFieldsMarkup}</Story>
</Canvas>
