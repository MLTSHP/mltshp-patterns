import shakeListMarkup from './examples/shake-list.html?raw';

/**
 * Appears in the admin area.
 */
export default {
	title: 'Components/Shake List',
	tags: ['autodocs'],
};

export const ShakeList = {
	render: () => shakeListMarkup,
};
